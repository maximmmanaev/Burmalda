# C#-глоссарий терминологии

Соответствие терминов из [docs/rules/terminology.md](../rules/terminology.md) именам
классов, полей и enum-значений в Unity/C#-коде. Единственный актуальный источник —
[PRD v7](../raw/BURMALDA_PRD_v7.md) (v4, v5 и v6 устарели); разделы 5–8, 13–15,
18–19 обновлены и разделы 20–26 добавлены в v7 (Знамения Шахты, сегментная
генерация, Ярусы Глубины, Испытания Шахты, Таблицы Рекордов, Пропуск
Экспедиции, Созвучия, Печати Боссов, Тёмный товар, детализация монетизации),
остальные разделы не менялись относительно предыдущих версий, но актуальный
текст — см. «Остальные разделы» в PRD v7. Стиль — Unity/C# (PascalCase для
публичных членов, см. [docs/rules/code-style.md](../rules/code-style.md)).

Список не придумывает сущностей сверх того, что описано в PRD — только даёт им
технические идентификаторы.

## Core-движение — тоннель, сетка, grid-trace (раздел 4.1)

Игрок тянет палец по соседним плитам, продвигаясь вперёд по тоннелю; нет
автоскролла камеры — давление идёт от распада плит позади (Decay, см. ниже),
не от таймера. Идентификаторы ниже реализуют саму сетку и приём/валидацию
трейс-хода; тип плиты (ловушка/валюта/артефакт) — предмет других систем/
спринтов и добавляется поверх `GridCoordinate` без изменения этого раздела.

| Термин (RU) | Раздел PRD | C#-идентификатор | Тип сущности |
|---|---|---|---|
| Плита (координата в сетке тоннеля) | 4.1 | `GridCoordinate` | struct (Core) |
| Плита (материализованная ячейка сетки, хранит и состояние распада) | 4.1 | `Tile` | класс (Core) |
| Тоннель / сетка плит | 4.1 | `TunnelGrid` | класс (Core) |
| Трейл grid-trace движения (тянешь палец по соседним плитам) | 4.1 | `GridTraceTrail` | класс (Movement) |
| Перевод точки мира (палец → плоскость пола тоннеля) в координату сетки | 4.1 | `WorldGridProjection` | struct (Movement) |
| Приём трейс-ввода пальцем | 4.1 | `GridTraceInputController` | класс (Movement) |

## Decay — распад плит трейла (разделы 4.1, 16)

Плиты позади игрока распадаются и разрушаются со временем — источник
постоянного давления вместо таймера на прохождение (раздел 4.1). Портировано
из `updateDecay()`/`tryAct()` в `legacy/burmolda_demo.html` (раздел 16:
перенос 2D top-down прототипа в 3D) без изменения констант тайминга; состояние
распада хранится прямо на `Tile` (см. выше), само тиканье — в `TrailDecaySystem`.

| Термин (RU) | Раздел PRD | C#-идентификатор | Тип сущности |
|---|---|---|---|
| Распад плит трейла (тиканье, ускорение со временем, разрушение по порогу) | 4.1, 16 | `TrailDecaySystem` | класс (Decay) |
| Плита разрушена распадом | 4.1 | `Tile.IsDestroyed` | поле (Core) |
| Порог распада плиты в секундах | 4.1 | `Tile.DecayThresholdSeconds` | поле (Core) |
| Тик распада каждый кадр (интеграция с забегом) | 4.1 | `TrailDecayController` | класс (Decay) |

## Камера тоннеля (раздел 16)

3D-камера от третьего лица следует за трейлом сверху-сзади вместо 2D-скролла
прототипа (раздел 16: перенос HTML top-down прототипа в Unity). Портировано
из camera-логики `legacy/burmolda_demo.html` (`cameraRow`/`cameraTargetRow`,
`draw()`) — константа сглаживания и офсет отставания по ряду перенесены
буквально; высота камеры над игроком — новое значение для 3D, аналога в
2D-прототипе нет (см. issue #8, скоуп увеличен с 2D до 3D третьего лица).
Персонаж не отображается — камера следует за
`GridTraceTrail.CurrentPosition` через `WorldGridProjection.ToWorldPosition`.

`TunnelCameraFollow.HeightOffset`/`PitchDegrees`/`IntroPitchDegrees`/
`IntroHeightOffsetZ` — публично изменяемые свойства (не только
конструкторские параметры): двигать `Transform` `TunnelCameraController`
напрямую в Scene view/инспекторе нельзя, он каждый кадр перезаписывается
результатом Follow — вместо этого меняются соответствующие поля в
инспекторе `TunnelCameraController`, которые прокидываются в `Follow`
каждый кадр и применяются вживую, без рестарта забега.

Top-down интро на старте забега: `TargetRotation`/`CurrentRotation`
вычисляются живьём как `Lerp(IntroPitchDegrees, PitchDegrees, scale)`, где
`scale` — общий приватный `ComputeCatchUpScale(playerRow)`
(`caughtUpDistance/TrailingRowsBehindPlayer`), тот же паттерн, что был у
старой убранной `LookAheadRowsBeyondPlayer`-логики точки взгляда (теперь
для угла, не для точки взгляда). Решает: на фиксированном игровом
`PitchDegrees` с первого кадра стартовая плитка физически оказывалась вне
кадра (трейлинг-ряд камеры на старте совпадает с игроком). `IntroPitchDegrees`
(дефолт 70°) — не 85°: на более крутом интро FOV, откалиброванный под
устоявшийся `PitchDegrees`, раздувает ближнюю плитку и роняет дальние ряды
за верхний край почти сразу (см. `CurrentPitchDegrees` ниже).

Отдельный, геометрический (не про угол) конфликт того же top-down интро:
`HeightOffset.Z`, применённый с первого кадра, физически уводит камеру
мимо стартовой плиты (трейлинг-ряд ещё клампится к 0, а Z уже
"устоявшийся") — плита уходит за НИЖНИЙ край. `IntroHeightOffsetZ`
(дефолт -1, подобран численно через `WorldToViewportPoint`) интерполируется
ТЕМ ЖЕ `ComputeCatchUpScale`, что и pitch, но только для Z-компоненты
`HeightOffset` — X/Y не участвуют.

`TunnelCameraFollow.CurrentPitchDegrees` — то же число, что в
`TargetRotation.eulerAngles.x`, но напрямую как градусы, без раскрытия
кватерниона. Нужно `TunnelCameraController`, который пересчитывает
горизонтальный FOV КАЖДЫЙ кадр (не только на (пере)сборке следования) от
этого живого pitch — иначе FOV, откалиброванный один раз под устоявшийся
`PitchDegrees`, не годится для геометрии top-down интро (сильно другой pitch).

`TunnelCameraFraming.ComputeDesiredHorizontalFovDegrees` берёт депт до
опорного ряда ВДОЛЬ ОСИ ВЗГЛЯДА камеры (`cameraHeight*sin(pitch) +
groundDistanceToRow*cos(pitch)`), не по прямой — обязательное условие
того, чтобы `Pitch Degrees` был по-настоящему тюнящимся полем (см.
`ComputeSteadyStateGroundDistanceToReferenceRow`/`FramingReferenceRowsBehindPlayer`
ниже — калибровка ширины обзора от устоявшегося режима следования, не от
вырожденного старта забега; `groundDistanceToRow` зависит только от
`HeightOffset.Z`, не от pitch, поэтому его саму по себе не нужно
пересчитывать по ходу top-down интро).

| Термин (RU) | Раздел PRD | C#-идентификатор | Тип сущности |
|---|---|---|---|
| Плавное следование камеры за трейлом (позиция и поворот) | 16 | `TunnelCameraFollow` | класс (Movement) |
| Тик следования камеры каждый кадр (интеграция с забегом) | 16 | `TunnelCameraController` | класс (Movement) |

## Ловушки и рычаги (раздел 4.2)

| Термин (RU) | Раздел PRD | C#-идентификатор | Тип сущности |
|---|---|---|---|
| Заблокированная плита (статичное препятствие, видно заранее, обходится) | 4.2 | `Tile.IsBlocked` / `Tile.MarkBlocked()` | поле / метод (Core) |
| Смертельная ловушка (яма/лава/взрыв — общий тип) | 4.2 | `LethalTrapType` | enum (Core) |
| Яма (статичная) | 4.2 | `LethalTrapType.Pit` | enum-значение |
| Лава (статичная) | 4.2 | `LethalTrapType.Lava` | enum-значение |
| Взрыв (динамическая мгновенная) | 4.2 | `LethalTrapType.Explosion` | enum-значение |
| Плита помечена смертельной ловушкой | 4.2 | `Tile.LethalTrap` / `Tile.MarkLethalTrap()` | поле / метод (Core) |
| Попытка шагнуть на смертельную ловушку (ход не засчитывается) | 4.2 | `GridTraceTrail.LethalTrapTriggered` | событие (Movement) |
| Процедурная расстановка препятствий по плитам сетки (аналог `genTileType()` прототипа; PRD v7 §21 — заменяется сегментной генерацией, см. ниже) | 4.2 | `TunnelObstacleGenerator` | класс (Core) |
| Материализация плит на несколько рядов вперёд игрока (препятствия «видны заранее») | 4.2 | `TunnelGridReveal` | класс (Movement) |
| Плита-триггер динамической мгновенной ловушки (взрыв) и связанная плита-цель | 4.2 | `Tile.ExplosiveTrapTarget` / `Tile.MarkExplosiveTrapTrigger()` | поле / метод (Core) |
| Активация ловушки при проходе трейла через плиту-триггер (взрыв) | 4.2 | `ExplosiveTrapArmingSystem` | класс (Movement) |
| Подвижная ловушка с таймингом (стрела/лезвие — общий тип) | 4.2 (v5) | `TimedTrapType` | enum (Core) |
| Стрела | 4.2 (v5) | `TimedTrapType.Arrow` | enum-значение |
| Лезвие | 4.2 (v5) | `TimedTrapType.Blade` | enum-значение |
| Плита-триггер ловушки с таймингом и связанная плита-цель | 4.2 (v5) | `Tile.TimedTrapTarget`/`Tile.TimedTrapKind` / `Tile.MarkTimedTrapTrigger()` | поле / метод (Core) |
| Плита-цель физически опасна прямо сейчас (снаряд/лезвие проходит) | 4.2 (v5) | `Tile.IsTimedTrapActive` / `Tile.ArmTimedTrap()` / `Tile.DisarmTimedTrap()` | поле / методы (Core) |
| Попытка шагнуть на активную ловушку с таймингом (ход не засчитывается) | 4.2 (v5) | `GridTraceTrail.TimedTrapTriggered` | событие (Movement) |
| Тайминг активации/деактивации ловушки по реальному времени (issue #45) | 4.2 (v5) | `TimedTrapSystem` | класс (Movement) |
| Рычаг (плита; не ловушка) | 4.2/21 | `SegmentTileType.Lever` | enum-значение (Generation) |
| Плита рычага активирована, координаты ворот бокового прохода | 4.2/21 | `Tile.IsLever` / `Tile.LeverGateTargets` / `Tile.MarkLever()` | поле / метод (Core) |
| Плита бокового прохода, закрытая до активации рычага | 4.2/21 | `Tile.IsGated` / `Tile.IsLeverGateOpen` / `Tile.MarkGated()` / `Tile.OpenLeverGate()` | поле / методы (Core) |
| Активация рычага при проходе трейла через него (issue #51) | 4.2/21 | `LeverActivationSystem` | класс (Generation) |

Попадание в ловушку по PRD должно запускать d20-испытание (раздел 9) — см.
следующий раздел про временную замену без d20. Рычаги (#51) реализованы как
элемент шаблона сегмента (сегмент «зал-с-рычагом» в `SegmentTemplateCatalog`),
а не отдельно расставляемой по сетке сущностью — см. раздел «Сегментная
генерация трасс» ниже.

**PRD v7 §21**: `TunnelObstacleGenerator` (побитовая расстановка per-tile, см.
выше) — временное решение Спринта 3, задокументированное как черновое ещё на
момент реализации. PRD v7 заменяет его сегментной генерацией (авторские
шаблоны-сегменты вместо случайного броска на каждую отдельную плиту, см.
раздел «Сегментная генерация трасс» ниже) — при реализации §21
`TunnelObstacleGenerator`/`TunnelGridReveal`/`ExplosiveTrapArmingSystem`/
`TimedTrapSystem` ожидаемо будут переписаны или поглощены новыми классами
(`SegmentTemplate`/`SegmentRowProvider`), как уже не раз происходило с
временными решениями в этом проекте (см. `RunState`/`RunController` из
раздела «Смерть и рестарт» — тот же паттерн, другая система).

`TunnelObstacleGenerator`/`TunnelGridReveal`/связки триггер→цель — частоты
(5% заблокировано, 1.5% яма, 1.5% лава, 3% триггер взрыва, 1.5%/1.5% триггер
стрелы/лезвия) черновые (block/pit/частота триггера взрыва — буквально или
по аналогии из прототипа, деление яма/лава и частота ловушки с таймингом —
решения агента без прототипа-прецедента), финальные значения — предмет
плейтеста баланса (Спринт 10, см. `docs/rules/forbidden-actions.md`). Цель
триггера — жёстко плита сразу впереди по глубине (тот же столбец) — тоже
решение агента, PRD не уточняет направление/радиус детальнее. Тайминг
`TimedTrapSystem` (`WindUpSeconds`=0.6с, `ActiveSeconds`=0.8с) — тоже
черновой, PRD говорит лишь «несколько мгновений»; первая версия — одна
плита-цель с окном времени, а не снаряд, движущийся по нескольким плитам
подряд (упрощение по решению владельца продукта).

## Множитель добычи (раздел 4.3)

«Множитель растёт по кривой в зависимости от количества уникальных
собранных плит трейла, с бонусом за не-прямолинейные пути». Кривая
(`MCURVE`/`getM` в `legacy/burmolda_demo.html`) перенесена буквально, как и
у Decay/`TunnelObstacleGenerator`. Реагирует на `GridTraceTrail.Advanced`
(только уникальные плиты, не на повторные шаги, #61) — ровно то, что
требует формулировка PRD «уникальных собранных».

| Термин (RU) | Раздел PRD | C#-идентификатор | Тип сущности |
|---|---|---|---|
| Кривая множителя от эффективной длины трейла | 4.3 | `MultiplierCurve.GetMultiplier()` | класс (Core) |
| Множитель после исчерпания кривой | 4.3 | `MultiplierCurve.MaxMultiplier` | константа (Core) |
| Число смен направления пути (бонус за не-прямолинейные пути) | 4.3 | `TrailMultiplierSystem.TurnCount` | поле (Movement) |
| Текущий множитель добычи забега | 4.3 | `TrailMultiplierSystem.CurrentMultiplier` | поле (Movement) |
| Интеграция с забегом (пересборка на `RunStarted`) | 4.3 | `TrailMultiplierController` | класс (Movement) |

Бонус скорости роста числа поворотов от Талисмана (`multiplierSpeedBonus()`,
`t2` в прототипе) не перенесён — соответствующего Талисмана в проекте ещё
нет (Спринт 5, эквивалентно множителю ×1). Как и у Decay/ловушек, применение
множителя к реальным Монетам/Кристаллам Маны — предмет Валют (Спринт 4),
здесь только сам множитель.

## Смерть, рестарт и d20 (раздел 9)

Issue #24 (Спринт 7) заменил временную упрощённую версию: `RunState` теперь
бросает `D20.D20Trial` вместо мгновенной смерти от ловушки/обвала плиты (см.
раздел «D20 — Испытание Шахты» выше). Поражение от Босса — отдельный путь,
`RunState.ReportBossDefeat()`, d20 не бросает (PRD v7 §8.3).

| Термин (RU) | Раздел PRD | C#-идентификатор | Тип сущности |
|---|---|---|---|
| Смерть игрока | 9 | `RunState` / `RunState.Died` | класс / событие (RunLifecycle) |
| Жив ли игрок в текущем забеге | 9 | `RunState.IsAlive` / `GridTraceInputController.IsAlive` | поле |
| Разрешение опасности через d20 вместо мгновенной смерти | 9 | `RunState` (приватный `ResolveHazard`) | метод |
| Поражение от Босса (d20 не применяется) | 8 (v7) | `RunState.ReportBossDefeat()` | метод |
| Событие каждого броска d20 (для будущего UX-фидбека, PRD §19) | 9 | `RunState.D20Resolved` | событие |
| Рестарт забега (мир перегенерируется заново) | — | `GridTraceInputController.Restart()` | метод (Movement) |

Источники опасности: `GridTraceTrail.LethalTrapTriggered` (ловушка),
`GridTraceTrail.TimedTrapTriggered` (ловушка с таймингом) и
`TrailDecaySystem.TileDestroyed` для текущей позиции игрока (обрушение плиты
под ногами) — по аналогии с `attemptDeath()` в `legacy/burmolda_demo.html`.

## Валюты (раздел 5)

Реализовано (issues #12–#14): временные (в забеге) Кристаллы Маны/Ключи/
Монеты и постоянные Монеты/Кристаллы структурно однотипны внутри своей
пары — временные валюты используют общий `RunCurrencyAccumulator`,
постоянные — общий `PersistentWallet`; конкретная роль (какая именно
валюта) задаётся тем, что именно её пополняет
(`TrailCoinSystem`/`TrailTileCurrencySystem`), не самим классом-хранилищем.

| Термин (RU) | Раздел PRD | C#-идентификатор | Тип сущности |
|---|---|---|---|
| Временный накопитель одной валюты забега (общая реализация) | 5 | `RunCurrencyAccumulator` | класс (Currencies) |
| Постоянный кошелёк одной валюты (общая реализация) | 5 | `PersistentWallet` | класс (Currencies) |
| Кристаллы Маны текущего забега | 5 | `CurrencyController.RunManaCrystals` | поле (`RunCurrencyAccumulator`) |
| Ключи текущего забега | 5 | `CurrencyController.RunKeys` | поле (`RunCurrencyAccumulator`) |
| Монеты текущего забега (ещё не зафиксированы) | 5 | `CurrencyController.RunCoins` | поле (`RunCurrencyAccumulator`) |
| Постоянный баланс Монет | 5 | `CurrencyController.Coins` | поле (`PersistentWallet`) |
| Постоянный баланс Кристаллов (донат) | 5 | `CurrencyController.Crystals` | поле (`PersistentWallet`) |
| Плита-источник Кристаллов Маны/Ключей | 5 | `Tile.IsManaSource`/`Tile.IsKeySource` | поле (Core) |
| Начисление Монет за собранную плиту (база × текущий множитель) | 5, 4.3 | `TrailCoinSystem` | класс (Currencies) |
| Сбор валюты с плиток-источников (Мана/Ключи) | 5 | `TrailTileCurrencySystem` | класс (Currencies) |
| Интеграция валют с забегом | 5 | `CurrencyController` | класс (Currencies) |

Формула Монет — буквальный порт `base*multi` из `legacy/burmolda_demo.html`
(`base`=1, без бонуса Талисмана — Спринт 5). Rare/epic/legendary-бонус к
базе (PRD v4 §3) не перенесён — не в явном тексте issue #13, известный
пробел. `ManaPerSource`=1 — порт `runManaCrystals++`; `KeysPerSource`=1 —
нет прецедента в прототипе (Ключи введены только в v6), черновое значение,
предмет баланса (Спринт 10). Постоянные кошельки живут только в памяти
сессии — сохранение на диск ещё не реализовано (issue #107, Спринт 9).
Фиксация `RunCoins`/`RunManaCrystals`/`RunKeys` в постоянные кошельки при
успешном возврате в лагерь не подключена — самого "возврата в лагерь" в
проекте ещё нет (Спринт 8); `PersistentWallet.Deposit()` публичен именно
для этого будущего подключения.

## Система артефактов (раздел 6)

Реализовано (issues #15–18, #79). `Idol`/`Totem`/`Amulet`/`Talisman`/`Rune`/
`Relic` — конкретные подклассы абстрактного `Artifact` (Id/Name/Category/
Tags). Числовые эффекты Амулетов/Талисманов (иммунитет к ловушкам, бонусы
к Мане/Ключам) не применяются автоматически к Currencies/RunLifecycle —
Алтарь как источник выдачи (Спринт 6) и d20 (Спринт 7) ещё не реализованы;
`EffectDescription` хранит только текстовое описание.

| Термин (RU) | Раздел PRD | C#-идентификатор | Тип сущности |
|---|---|---|---|
| Артефакт (общий термин для Идола/Тотема/Амулета/Талисмана/Руны/Реликвии) | 6 | `Artifact` | абстрактный класс |
| Идол (2 слота, 2 прокачиваемых Руной пассива) | 6 | `Idol` | класс |
| 2 постоянных слота Идолов | 6 | `IdolLoadout` | класс |
| Тотем (держатель активной способности) | 6, 12 | `Totem` | класс |
| Амулет | 6 | `Amulet` | класс |
| Талисман | 6 | `Talisman` | класс |
| Руна (с линией прокачки — для Созвучия «Единство») | 6 | `Rune` | класс |
| Реликвия | 6, 8 | `Relic` | класс |
| Коллекция (использованный хотя бы раз артефакт фиксируется навсегда) | 6 | `ArtifactCollection` | класс |
| Пул артефактов (что разблокировано к выпадению/покупке) | 6 | `ArtifactPool` | класс |
| Временный билд забега (Амулеты/Талисманы, сбрасывается) | 6 | `RunArtifactLoadout` | класс |
| Каталог видов артефактов (5 примеров issue #17) | 6 | `ArtifactCatalog` | класс |
| Тег артефакта (Мана/Ключи/Жадность/Защита/Ход) | 6 (v7) | `ArtifactTag` | enum |
| Созвучие (бонус за совпадение тегов, кап 2 активных) | 6 (v7) | `ResonanceType` | enum |
| Вычисление активных Созвучий по порядку получения артефактов | 6 (v7) | `ResonanceCalculator` | класс |

## Достижения (разделы 1, 6)

Реализован механизм (issue #50, PRD v6 раздел 1): условие-предикат →
разлок артефакта в `ArtifactPool` → событие `Granted` (хук под "яркое
визуальное оповещение" — сама нотификация вне скоупа, в проекте нет
UI-слоя). Каталог из 10 конкретных Достижений (объём релиза,
docs/wiki/roadmap.md) НЕ создан — ни PRD, ни issue #50 не называют ни
одного конкретного условия/названия/артефакта-награды; см. doc-комментарий
`AchievementDefinition`. Тиры I/II/III (PRD v7 раздел 6.3, issue #85) —
вынесены в Спринт 13, здесь не реализованы.

| Термин (RU) | Раздел PRD | C#-идентификатор | Тип сущности |
|---|---|---|---|
| Достижение (условие + артефакт-награда) | 1, 6 | `Achievements.AchievementDefinition` | класс |
| Отслеживание получения Достижений | 1, 6 | `Achievements.AchievementTracker` | класс |
| Получение Достижения (хук под визуальное оповещение) | 1, 6 | `Achievements.AchievementTracker.Granted` | событие |

## Алтарь и Ритуал (раздел 7)

Реализовано (issues #19–21, #81). Клетка-Алтарь — плита (`Tile.IsAltar`,
как и Рычаг/источники валют — не отдельная игровая сущность, а состояние
плиты). Ритуал — обычный C#-класс (не MonoBehaviour), создаётся
`AltarTriggerSystem` по достижении Алтаря; UI витрины не реализован —
проект пока нигде не строит UI (см. другие системы). Тёмный товар (PRD v7
§7) вынесен за релиз при пересборке плана под release-first (issue #103,
Спринт 13) — `HiddenOffer` не реализован.

| Термин (RU) | Раздел PRD | C#-идентификатор | Тип сущности |
|---|---|---|---|
| Клетка-Алтарь | 7 | `Tile.IsAltar` / `Tile.MarkAltar()` | поле / метод (Core) |
| Плитка-шаблон сегмента с Алтарём | 7, 21 | `SegmentTileType.Altar` | enum-значение (Generation) |
| Запуск Ритуала по достижении Алтаря (проход мимо без Ключей — событие не срабатывает) | 7 | `AltarTriggerSystem` | класс (Altar) |
| Интеграция с забегом, постоянные Пул/Коллекция | 7 | `AltarController` | класс (Altar) |
| Ритуал (витрина: основной слот + Сундук Рун, реролл, продажа обратно) | 7 | `Ritual` | класс (Altar) |
| Сундук (общий термин для витрины Алтаря) | 7 | `Chest` | абстрактный класс (Altar) |
| Сундук Рун | 7 | `RuneChest` | класс (Altar) |
| Сундук Талисманов | 7 | `TalismanChest` | класс (Altar) |
| Сундук Амулетов | 7 | `AmuletChest` | класс (Altar) |
| Сундук Реликвий | 7 | `RelicChest` | класс (Altar) |
| Цена реролла (первый — база, дальше ×1.5) | 7 (v7) | `RerollPricing` | класс (Altar) |
| Продажа артефакта обратно Алтарю за Ключи | 7 | `Ritual.TrySell()` | метод |
| Превью потери Созвучия при продаже | 7 (v7) | `Ritual.PreviewResonanceLossOnSell()` | метод |
| Подсветка Созвучия, которое соберётся при покупке | 7 (v7) | `Ritual.PreviewResonanceGainOnPurchase()` | метод |
| Индикатор энергии до Босса (витрина и HUD) | 7 (v7) | `ManaToBossIndicator` | класс (Altar) |
| Тёмный товар (закрытый слот витрины) — вынесен в Спринт 13 (пост-релиз) | 7 (v7) | `HiddenOffer` | не реализовано |

Применение Руны к конкретному пассиву Идола/Тотема — выбор игрока (UI,
которого ещё нет); `Ritual` только выдаёт `Rune`, применение — существующие
`Idol.UpgradePassiveA()`/`UpgradePassiveB()`/`Totem.UpgradeLevel()`
(Спринт 5), новой связующей сущности не потребовалось. Цены сундуков
(`Ritual.TalismanOrAmuletChestCost`=80, `RuneChest.Cost`=50 в `RerollPricing.BaseCost`)
и коэффициент продажи (`Ritual.SellCoefficient`=0.5) — черновые значения,
предмет баланса (Спринт 10). "2 Алтаря перед каждым Боссом" (PRD v7 §21) не
подключено — ни Алтаря-в-составе-фиксированного-потока, ни Босса (Спринт 7)
для привязки ещё нет.

## Боссы (раздел 8)

Реализовано (issues #22, #82, #83). Точка Босса — плита (`Tile.IsBoss`), как
Алтарь/Рычаг. Требуемая энергия растёт от Яруса Глубины
(`Progression.RunDepthTier`, раздел «Ярусы Глубины» ниже) — заменило
черновую заглушку по ряду сетки из Спринта 7, как только Ярусы появились.
Печати Боссов и Эхо Босса (PRD v7 §8.1/8.4) вынесены за релиз при
пересборке плана под release-first (issue #104, Спринт 13) — `BossSeal` не
реализован.

| Термин (RU) | Раздел PRD | C#-идентификатор | Тип сущности |
|---|---|---|---|
| Точка встречи с Боссом | 8 | `Tile.IsBoss` / `Tile.MarkBoss()` | поле / метод (Core) |
| Плитка-шаблон сегмента с Боссом | 8, 21 | `SegmentTileType.Boss` | enum-значение (Generation) |
| Босс (порог энергии, разрешение встречи) | 8 | `Boss` | класс (Boss) |
| Результат встречи (победа/поражение, Перелив, шанс редкой Реликвии) | 8 | `BossEncounterOutcome` | класс (Boss) |
| Перелив энергии сверх порога Босса (конвертация в Монеты) | 8 (v7) | `Boss.OverflowToCoinsRate` | константа |
| Флаг первой победы за прохождение (разлок пула Амулетов/Талисманов) | 6, 8 | `FirstBossVictoryTracker` | класс (Boss) |
| Разрешение встречи по достижении плиты, интеграция с забегом | 8 | `BossEncounterSystem` / `BossController` | класс (Boss) |
| Печать Босса (модификатор встречи по Ярусу и seed) — вынесена в Спринт 13 | 8 (v7) | `BossSeal` | не реализовано |

## D20 — Испытание Шахты (раздел 9)

Реализовано (issue #24). Заменяет временную упрощённую замену без броска
(см. «Смерть и рестарт» ниже) — `RunState` теперь бросает d20 через
ловушки/обвал плиты вместо мгновенной смерти. Knockback реализован
буквально — телепорт к последнему пройденному Алтарю через новый
`GridTraceTrail.TeleportTo()` (не ход игрока: не проверяет соседство/
препятствия, не материализует новых плит). До первого пройденного Алтаря
в забеге "последний Алтарь" по умолчанию — стартовая плита забега.

| Термин (RU) | Раздел PRD | C#-идентификатор | Тип сущности |
|---|---|---|---|
| d20-испытание | 9 | `D20Trial` | класс (D20) |
| Результат броска (15–20 — везение, продолжение без потерь) | 9 | `D20Outcome.Fortune` | enum-значение |
| Результат броска (10–14 — откат к последнему Алтарю) | 9 | `D20Outcome.Knockback` | enum-значение |
| Результат броска (1–9 — смерть) | 9 | `D20Outcome.Death` | enum-значение |
| Принудительный перенос текущей позиции (не ход игрока) | 9 | `GridTraceTrail.TeleportTo()` | метод (Movement) |

## Возврат и кэш-аут (раздел 10)

Реализовано (issues #25, #26). «Путь назад — не телепорт» реализовано
буквально тем, что НЕ добавлено: `GridTraceTrail` уже поддерживает шаг в
любом направлении (8-соседство, повтор непройденных плит, #61) — путь
назад проходит тем же `TryAdvanceTo`, никакой новой механики движения не
понадобилось. Успешный возврат фиксирует только Монеты — Кристаллы
(донат-валюта) не собираются на карте в текущей модели валют (v6+, раздел
5), формулировка v4 §10 про их фиксацию при возврате устарела.

| Термин (RU) | Раздел PRD | C#-идентификатор | Тип сущности |
|---|---|---|---|
| Кэш-аут — фиксация чекпоинта на Алтаре | 10 | `Camp.CashOutSystem` | класс |
| Чекпоинт/откат валюты забега | 10 | `Currencies.RunCurrencyAccumulator.Checkpoint()`/`RevertToCheckpoint()` | методы |
| Начало возврата в лагерь ("Кнопка «Вернуться»") | 10 | `Camp.ReturnJourneySystem.BeginReturn()` | метод |
| Успешный возврат (достижение ряда 0) | 10 | `Camp.ReturnJourneySystem.Returned` | событие |
| Смерть в пути назад — откат к чекпоинту | 10 | `Camp.ReturnJourneySystem.HandleDeathDuringReturn()` | метод |

## Лагерь (раздел 11)

Реализовано (issue #27). PRD не называет конкретные "топовые" артефакты —
`Camp` принимает Id/цену снаружи, механизм общий, не привязан к
захардкоженному списку.

| Термин (RU) | Раздел PRD | C#-идентификатор | Тип сущности |
|---|---|---|---|
| Лагерь | 11 | `Camp.Camp` | класс |
| Апгрейд пассива Идола/способности Тотема за Монеты | 11 | `Camp.TryUpgradeIdolPassiveA()`/`TryUpgradeIdolPassiveB()`/`TryUpgradeTotem()` | методы |
| Разлок вида артефакта в общий пул за Кристаллы | 11 | `Camp.TryUnlockArtifact()` | метод |
| Расширение пула через принесённую Реликвию | 11 | `Camp.OpenRelic()` | метод |
| Интеграция с забегом | 11 | `Camp.CampController` | класс |

## Сохранение прогресса и офлайн-режим (issue #107)

Сквозная инфраструктура, не отдельная система PRD — не привязана к номеру
раздела. `SaveController` сохраняет/загружает JSON-файл в
`Application.persistentDataPath` (`Start()`/`OnApplicationPause(true)`/
`OnApplicationQuit()`). `ProgressSnapshot.Capture()`/`Apply()` — чистые
статические функции над уже существующими постоянными объектами
(`PersistentWallet` Монет/Кристаллов, `ArtifactCollection`, `ArtifactPool`,
`DepthRecord`, `Boss.FirstBossVictoryTracker`), без побочных эффектов на
файловую систему — тестируются без реального Unity Editor. Идолы/Тотем/
Руны не сохраняются: в проекте нет каталога конкретных постоянных
экземпляров этих артефактов (см. пробел в разделе «Активная способность
Тотема» ниже) — честно задокументированный пробел, а не забытая фича.

| Термин (RU) | Раздел PRD | C#-идентификатор | Тип сущности |
|---|---|---|---|
| Снимок сохраняемых данных | — | `Persistence.SaveData` | класс |
| Снятие/применение снимка к постоянным объектам | — | `Persistence.ProgressSnapshot` | класс |
| Сохранение/загрузка файла в `persistentDataPath` | — | `Persistence.SaveController` | класс |

## Активная способность Тотема (раздел 12)

| Термин (RU) | Раздел PRD | C#-идентификатор | Тип сущности |
|---|---|---|---|
| Активная способность (свойство Тотема) | 12 | `Totem.ActiveAbility` | поле |
| Тип активной способности (общий enum) | 12 | `TotemAbilityType` | enum |
| Рывок | 12 | `TotemAbilityType.Dash` | enum-значение |
| Пробой | 12 | `TotemAbilityType.Breach` | enum-значение |
| Неуязвимость | 12 | `TotemAbilityType.Invulnerability` | enum-значение |
| Второе дыхание (руна) | 8, 12 | `TotemAbilityType.SecondWind` | enum-значение |
| Заряд «Второго дыхания» — один за забег (issue #23) | 8, 12 | `RunLifecycle.SecondWindCharge` | класс |
| Заряд активной способности, копится по ходу забега (issue #28) | 12 | `Artifacts.TotemChargeSystem` | класс |
| Точка входа активации Рывка/Пробоя/Неуязвимости (issue #28) | 12 | `Artifacts.TotemAbilityActivationSystem` | класс |
| Одноразовый проход сквозь заблокированную плиту — эффект Пробоя (issue #28) | 12 | `Movement.GridTraceTrail.PrimeBreach()` | метод |
| Временная приостановка распада — эффект Неуязвимости (issue #28) | 12 | `Decay.TrailDecaySystem.Suspend()` | метод |

`SecondWindCharge` реализован (заряд, правило "один раз за забег"), но не
подключён к реальному триггеру — с появлением `Camp.ReturnJourneySystem`
(issue #26, Спринт 8) механика "провала возврата" в проекте уже есть, но
`SecondWindCharge.TryConsume()` пока не вызывается из
`ReturnJourneySystem.HandleDeathDuringReturn` — эта проводка не входила ни
в issue #23, ни в issue #26/#28 явно, оставлена как известный, отдельно не
заведённый пробел.

`TotemChargeSystem`/`TotemAbilityActivationSystem` реализуют саму логику
заряда и применения эффекта (Спринт 9, issue #28), но не подключены
MonoBehaviour-контроллером к `GridTraceInputController.RunStarted` — в
проекте нет понятия "экипированный на этот забег Тотем" (нет своего
лоадаута, в отличие от `IdolLoadout`), и нет UI/детектора двойного тапа
поверх экрана ввода трейса. См. doc-комментарий класса
`TotemAbilityActivationSystem` — оба пробела задокументированы там же.
Дистанция Рывка (3 плиты) — черновое значение "на глазок" (в прототипе
Рывок не реализован вовсе), финальное значение задаст плейтест баланса
(Спринт 10).

## Монетизация (раздел 13)

| Термин (RU) | Раздел PRD | C#-идентификатор | Тип сущности |
|---|---|---|---|
| Монетизационный оффер (общий термин) | 13 | `MonetizationOffer` | класс |
| Near-miss revive-оффер | 13 | `ReviveOffer` | класс |
| Гача-пак артефактов | 13 | `ArtifactGachaPack` | класс |
| Rewarded-размещение (pull, свой суточный кап у каждого) | 13 (v7) | `RewardedPlacement` | enum |
| Счётчик гарантии гачи (pity — гарантированный топ-тир через 30 паков) | 13 (v7) | `GachaPityCounter` | класс |

## Знамения Шахты (раздел 20)

Добровольные модификаторы забега — усложнение в обмен на награду, выбираются
в Лагере перед спуском; действуют один забег. Комбинаторика со Знамениями,
Созвучиями (раздел 6) и Рунами требует единой точки применения всех
множителей забега — `RunModifiers` — иначе эффекты расползутся по системам
и станут неотлаживаемыми (см. PRD v7 §20, «Риски»).

| Термин (RU) | Раздел PRD | C#-идентификатор | Тип сущности |
|---|---|---|---|
| Знамение Шахты | 20 | `Omen` | класс |
| Набор модификаторов забега (единая точка применения) | 20 | `RunModifiers` | класс |

## Сегментная генерация трасс (раздел 21)

Заменяет `TunnelObstacleGenerator`/`TunnelGridReveal` (см. «Ловушки и
рычаги» выше — оба помечены устаревшими в коде, но НЕ удалены: уже
привязаны к GameObject на `SampleScene.unity`, а трогать .unity-файлы
автономно запрещено, docs/rules/forbidden-actions.md) — тоннель собирается
из авторских шаблонов-сегментов на 5–8 рядов существующей сетки, выбор
детерминирован по seed забега (обычный забег — случайный, Испытание —
общий для всех игроков).

| Термин (RU) | Раздел PRD | C#-идентификатор | Тип сущности |
|---|---|---|---|
| Собственный seeded-RNG (независим от `UnityEngine.Random`) | 21 | `RunSeed` | класс (Generation) |
| Детерминированный seed Испытания (`f(дата, ярус)`) | 21, 23.1 | `RunSeed.ComputeTrialSeed()` | статический метод |
| Тип плиты внутри шаблона сегмента (переиспользует ловушки раздела 4.2) | 21 | `SegmentTileType` | enum (Generation) |
| Тег награды сегмента (Мана/Ключи/Монеты/артефакт) | 21 | `SegmentRewardTag` | enum (Generation) |
| Авторский шаблон сегмента (5–8 рядов, тег сложности 1–5) | 21 | `SegmentTemplate` | класс (Generation) |
| Проверка проходимости шаблона на этапе авторинга (не в рантайме) | 21 | `SegmentReachabilityValidator` | класс (Generation) |
| Выбор следующего шаблона по кривой сложности | 21 | `SegmentSelector` | класс (Generation) |
| Применение выбранных шаблонов к `TunnelGrid`, материализация заранее | 21 | `SegmentRowProvider` | класс (Generation) |
| Каталог авторских шаблонов | 21 | `SegmentTemplateCatalog` | класс (Generation) |
| Интеграция с забегом (пересборка на `RunStarted`) | 21 | `SegmentGenerationController` | класс (Generation) |

Каталог — 11 шаблонов (не ~30 на Ярус, требуемых PRD до релиза) — контент-долг,
задокументирован прямо в `SegmentTemplateCatalog` и в контент-чеклисте
релиза (Спринт 10, roadmap.md). Кривая сложности по Ярусам — заглушка от
номера ряда (`SegmentGenerationController.RowsPerDifficultyStep`=40 рядов на
+1 сложность, до 5), т.к. Ярусов Глубины в проекте ещё нет (Спринт 8) —
предмет замены и баланса позже, не молчаливая финальная цифра. Гарантия
фиксированного потока перед Боссом (2 Алтаря + жила Маны, §21) не
реализована — Алтарь (Спринт 6) теперь есть (`SegmentTileType.Altar`,
шаблон «зал-алтаря»), но Босса (Спринт 7) всё ещё нет, подключать саму
гарантию потока по-прежнему не к чему; `SegmentSelector`/`SegmentRowProvider`
уже поддерживают отбор по `SegmentRewardTag.Mana`, монтаж гарантии — задача
Спринта 7. `SegmentTileType.ManaSource`/`KeySource` (issue #12, Спринт 4) —
плитки шаблонов реально размечают `Tile.IsManaSource`/`IsKeySource` (см.
раздел «Валюты» ниже), не только несут тег сегмента: шаблоны «жила-маны»/
«россыпь-ключей» в `SegmentTemplateCatalog` дополнены такими плитками, как
и «зал-алтаря» — плиткой `SegmentTileType.Altar` (issue #19, Спринт 6).

| Термин (RU) | Раздел PRD | C#-идентификатор | Тип сущности |
|---|---|---|---|
| Seed забега | 21 | `RunSeed` | struct |
| Сегмент трассы | 21 | `TunnelSegment` | класс |
| Шаблон сегмента (авторский контент) | 21 | `SegmentTemplate` | ScriptableObject |
| Поставщик рядов сетки из сегментов | 21 | `SegmentRowProvider` | класс |

## Ярусы Глубины (раздел 22)

Реализовано в релизном объёме — 3 Яруса (issue #83, roadmap.md «Скоуп
релиза»). Каждый побеждённый Босс = граница Яруса. Требуемая энергия
следующего Босса растёт от ТЕКУЩЕГО (до победы) Яруса — см. раздел «Боссы».
Ярусы 4+, Печать Глубины и Новый Спуск — вынесены в Спринт 14 (пост-релиз,
issue #105) при пересборке плана под release-first; `DepthSeal`/`NewDescent`
не реализованы.

| Термин (RU) | Раздел PRD | C#-идентификатор | Тип сущности |
|---|---|---|---|
| Ярус Глубины текущего забега | 22 | `Progression.RunDepthTier` | класс |
| Постоянный рекорд глубины | 22 | `Progression.DepthRecord` | класс |
| Печать Глубины — вынесена в Спринт 14 | 22 | `DepthSeal` | не реализовано |
| Новый Спуск (prestige) — вынесен в Спринт 14 | 22 | `NewDescent` | не реализовано |

## Испытания Шахты (раздел 23)

Ежедневное — общий для всех игроков seed трассы + фиксированное Знамение
дня, зачётная только первая попытка. Еженедельное — 5 накопительных целей
из обычных забегов. Обе используют один и тот же `TunnelSegment`/`RunSeed`
из раздела 21.

| Термин (RU) | Раздел PRD | C#-идентификатор | Тип сущности |
|---|---|---|---|
| Ежедневное Испытание Шахты | 23 | `DailyTrial` | класс |
| Еженедельное Испытание Шахты | 23 | `WeeklyTrial` | класс |
| Серия участия в Испытаниях | 23 | `TrialStreak` | поле |

## Таблицы Рекордов (раздел 24)

Реализация — платформенные сервисы (Game Center / Google Play Games
Services), собственного бэкенда нет. Спринт 13, после публикации — см.
[Roadmap](roadmap.md).

| Термин (RU) | Раздел PRD | C#-идентификатор | Тип сущности |
|---|---|---|---|
| Таблица Рекордов | 24 | `Leaderboard` | класс |
| Тень лидера (реплей маршрута) | 24 | `GhostTrail` | класс |

## Пропуск Экспедиции (раздел 25)

Сезон — 4 недели, 50 уровней, содержимое обеих дорожек полностью
детерминировано (без рандома). Спринт 13, после публикации.

| Термин (RU) | Раздел PRD | C#-идентификатор | Тип сущности |
|---|---|---|---|
| Пропуск Экспедиции | 25 | `ExpeditionPass` | класс |
| Опыт Экспедиции (не валюта — не хранится в кошельке, не тратится) | 25 | `ExpeditionExperience` | поле |
