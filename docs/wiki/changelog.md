# Changelog

## 2026-08-06

- Организована трёхуровневая документация: `docs/raw/` (неизменяемые вводные пользователя), `docs/wiki/` (живая документация), `docs/rules/` (правила для агента, разбитые на отдельные файлы).
- Корневой `CLAUDE.md` сокращён до краткого файла со ссылкой на `docs/rules/index.md`.
- Добавлен PRD v4 (`docs/raw/BURMALDA_PRD_v4.md`) как неизменяемый источник истины.
- Разработка Burmalda разбита на 12 спринтов по 2 недели (от HTML-прототипа до релиза в сторы), для каждого создан [GitHub Milestone](https://github.com/maximmmanaev/Burmalda/milestones) и набор issues с метками `feature`/`balance`/`refactor`/`infra`. См. [Roadmap](roadmap.md).
- Добавлен CI-workflow `.github/workflows/unity-tests.yml` — прогон Unity Test Framework на каждый Pull Request через `game-ci/unity-test-runner@v4`. Пока в репозитории нет Unity-проекта, шаг тестов пропускается без падения джобы. См. [docs/rules/ci.md](../rules/ci.md).
- PRD обновлён до v5 (`docs/raw/BURMALDA_PRD_v5.md`): игра переведена в 3D (камера от третьего лица сверху, персонаж не отображается — виден только трейл плит), движение теперь визуально вперёд, а не вверх; механика ввода (трейс-выбор пальцем) не изменилась. Добавлен новый тип ловушки — активируемые подвижные (стрела/лезвие) с таймингом прохода. [Roadmap](roadmap.md) обновлён (ссылки на PRD переведены на v5, описания Спринтов 1–3 дополнены), 9 существующих issues получили пометку об изменениях, создан новый issue на подвижные ловушки в Спринте 3 (#45).
- Закрыты issues #3 и #5: добавлен [C#-глоссарий](csharp-glossary.md) (соответствие терминологии PRD именам классов/полей/enum для будущего Unity-кода, разделы 5–13), и `legacy/burmolda_demo.html` переименован под терминологию v4 — чекпоинты → Алтарь (открывает Ритуал), ключи → Кристаллы Маны; логика, баланс и вид (2D) прототипа не менялись.

## 2026-08-07

- Закрыт issue #4: организована структура `Assets/Scripts/` под архитектуру Burmalda — по одной пустой папке (с `.gitkeep`) на каждую систему из [C#-глоссария](csharp-glossary.md): `Core`, `Movement`, `Decay`, `Traps`, `Currencies`, `Artifacts`, `Altar`, `RelicHall`, `D20`, `Camp`, `Monetization`. Реальные классы не добавлялись — это задел под будущие спринты. См. `Assets/Scripts/README.md` (соответствие папок спринтам) и [Roadmap](roadmap.md).
- `.gitignore` дополнен записью `.DS_Store`.
- PRD обновлён до v6 (`docs/raw/BURMALDA_PRD_v6.md`): Кристаллы Маны переопределены (магическая энергия — очки рекорда/множителя И урон боссу), добавлена валюта Ключи (раздел 5); Зал Реликвии убран, заменён обязательным Боссом перед дальнейшим продвижением (2 Алтаря перед каждым, автобой лучом энергии, победа/поражение по накопленной Мане, раздел 8); добавлены рычаги — секретные боковые проходы к артефактам (раздел 4.2); добавлена система Достижений (раздел 1); в Алтаре теперь можно продавать артефакты обратно за Ключи (раздел 7); Талисманы/Амулеты разблокируются в общий пул только после первой победы над Боссом (раздел 6).
  - [C#-глоссарий](csharp-glossary.md) обновлён: добавлены `Keys` (Валюты), `Boss` (заменяет `RelicHall`, раздел «Зал Реликвии» переименован в «Боссы»), `Lever` (новый раздел «Ловушки и рычаги», 4.2), `Achievement` (новый раздел «Достижения», 1/6), `SellArtifact` (метод, Алтарь).
  - `Assets/Scripts/RelicHall/` удалена, добавлены `Assets/Scripts/Boss/` и `Assets/Scripts/Achievements/`; `Assets/Scripts/README.md` обновлён под новую структуру.
  - [Roadmap](roadmap.md) обновлён: Спринт 3 дополнен Рычагами, Спринт 4 — Ключами и новой ролью Кристаллов Маны, Спринт 5 — разблокировкой пула Талисманов/Амулетов и примерами артефактов, Спринт 6 — продажей артефактов обратно, Спринт 7 переименован в «Боссы и d20» с новым содержанием; Достижения добавлены в Спринт 9 (после системы артефактов, до Баланса/Монетизации — обоснование в roadmap.md).
  - GitHub: milestone 7 переименован в «Спринт 7: Боссы и d20»; issue #22 переписан под механику Босса; issues #12–14 (Спринт 4) и #19–21 (Спринт 6) точечно обновлены под новую терминологию v6 (Ключи вместо Кристаллов Маны как валюта Ритуала, продажа артефактов); созданы новые issues #50 (Достижения, Спринт 9) и #51 (Рычаги, Спринт 3).

## 2026-08-07 (продолжение)

- Исправлены устаревшие ссылки на PRD v5 в живой документации (`docs/wiki/roadmap.md`, `docs/wiki/csharp-glossary.md`) — теперь единственный указанный источник истины по фичам это PRD v6. Файлы `docs/raw/*.md` (v4/v5/v6) не трогались — они неизменяемые.
- Закрыт issue #6 (Спринт 2): портировано в Unity/C# базовое grid-trace движение из HTML-прототипа (`legacy/burmolda_demo.html`) — игрок тянет палец по соседним плитам сетки, продвигаясь вперёд по тоннелю (PRD 4.1). Персонаж не отображается, автоскролл камеры не реализуется (отдельная задача #8).
  - `Assets/Scripts/Core/`: `GridCoordinate` (координата плиты, 8-направленное соседство), `Tile`, `TunnelGrid` (сетка плит с ленивой материализацией рядов) — общая инфраструктура для будущих Decay/Traps поверх той же координаты.
  - `Assets/Scripts/Movement/`: `GridTraceTrail` (валидация и продвижение хода — соседняя, ещё не пройденная плита), `WorldGridProjection` (точка мира ↔ координата сетки; тоннель идёт вперёд по оси Z, поперёк — по оси X), `GridTraceInputController` (приём трейс-ввода пальцем через Input System, продвижение трейла).
  - Оба модуля оформлены как asmdef (`Burmalda.Core`, `Burmalda.Movement`) с отдельными EditMode Test-сборками; логика, тестируемая без сцены (валидность хода, соседство, проекция координат), покрыта Unity Test Framework.
  - [C#-глоссарий](csharp-glossary.md) дополнен новым разделом «Core-движение — тоннель, сетка, grid-trace» (раздел PRD 4.1).
- Закрыт issue #7 (Спринт 2): портирована в Unity/C# decay-система из HTML-прототипа — плиты трейла позади игрока распадаются и разрушаются со временем, создавая постоянное давление вместо таймера на прохождение (PRD 4.1, 16). Константы тайминга перенесены один в один из `updateDecay()`/`tryAct()` (`legacy/burmolda_demo.html`): базовый порог распада 6с + 0.18с за каждую плиту вглубь трейла, глобальное ускорение распада +0.025/с начиная с 1.
  - `Assets/Scripts/Core/Tile.cs` дополнен состоянием распада (`IsDestroyed`, `DecayElapsedSeconds`, `DecayThresholdSeconds`, `DecayProgress01`) и методами `BeginDecay`/`AdvanceDecay` — само тиканье реализует Decay, Tile только хранит и применяет результат.
  - `Assets/Scripts/Movement/GridTraceTrail.cs` дополнен событием `Advanced` (точка интеграции для Decay); `GridTraceInputController` дополнен свойством `Grid`. Публичный API #6 не менялся, существующие тесты остаются зелёными.
  - `Assets/Scripts/Decay/`: `TrailDecaySystem` (тиканье распада плит трейла, кроме стартовой) и `TrailDecayController` (MonoBehaviour, тикает каждый кадр) — asmdef `Burmalda.Decay` с EditMode Test-сборкой.
  - [C#-глоссарий](csharp-glossary.md) дополнен разделом «Decay — распад плит трейла» (PRD 4.1, 16).
- Добавлено новое правило для агента: [`docs/rules/implementation-workflow.md`](../rules/implementation-workflow.md) — перед реализацией любой задачи агент обязан сначала изучить архитектуру вертикально (весь путь фичи от входа до данных) и горизонтально (соглашения соседних систем), и только потом писать тест под критерии приёмки и лишь затем код к нему. `docs/rules/index.md` дополнен ссылкой.
- Закрыт issue #8 (Спринт 2): портирована в Unity/C# camera/scroll-логика тоннеля из HTML-прототипа — скоуп увеличен согласно PRD раздел 16 с 2D top-down скролла до 3D-камеры от третьего лица, следующей за игроком сверху-сзади (персонаж не отображается, виден только трейл плит). Реализовано по паттерну Movement/Decay (`docs/rules/implementation-workflow.md`): изучена архитектура вертикально/горизонтально, тесты написаны раньше кода.
  - `Assets/Scripts/Movement/TunnelCameraFollow.cs` — плавное следование камеры за `GridTraceTrail.CurrentPosition` (через `WorldGridProjection.ToWorldPosition`) с подпиской на событие `Advanced`, аналогично `TrailDecaySystem`. Константа сглаживания (`0.045` за тик) и офсет отставания камеры (5 рядов позади игрока) перенесены буквально из `cameraRow`/`cameraTargetRow` (`draw()`/`tryAct()`, `legacy/burmolda_demo.html`); высота камеры над игроком — новое значение для 3D, в 2D-прототипе аналога нет.
  - `Assets/Scripts/Movement/TunnelCameraController.cs` — тонкий `MonoBehaviour`-драйвер (тикает следование каждый кадр, применяет позицию/поворот к своему `Transform`), по тому же паттерну, что `TrailDecayController`. Привязка к Main Camera на сцене — вручную пользователем, не входит в эту задачу.
  - `GridTraceInputController` дополнен свойством `Projection` (доступ к `WorldGridProjection` забега для камеры). Публичный API #6/#7 не менялся, существующие тесты остаются зелёными.
  - Тесты (EditMode, написаны до кода): целевая позиция/поворот камеры на старте забега, клампинг офсета в первых 5 рядах, отставание на 6+ рядах, столбец без задержки при боковом ходе, буквальное применение константы сглаживания за один и много тиков, поворот камеры (смотрит вперёд/вниз при подъёме над игроком), откат к `Quaternion.identity` при нулевом направлении, отписка через `Dispose`.
  - [C#-глоссарий](csharp-glossary.md) дополнен разделом «Камера тоннеля» (PRD раздел 16); вводная Core-движения скорректирована (камера больше не «предмет других систем» — реализована здесь).
- Закрыт issue #58 (метка `infra`, вне очереди спринтов): минимальный debug-визуал сетки тоннеля в рантайме — не финальный арт, а способ ручного тестирования уже готовой логики Core/Movement/Decay глазами, а не только через Test Runner. Реализовано по `docs/rules/implementation-workflow.md`: архитектура изучена вертикально/горизонтально (по паттерну `TrailDecaySystem`/`TrailDecayController`), тест на маппинг состояние→цвет написан раньше кода.
  - `TunnelGrid` дополнен событием `TileMaterialized` (срабатывает при первой материализации новой плиты в `GetOrCreateTile`, не при повторных обращениях) — точка интеграции для визуала, аддитивно, публичный API #6/#7/#8 не менялся.
  - `Assets/Scripts/DebugVisuals/` (новая папка, debug-инфраструктура — не система из PRD, не добавлена в C#-глоссарий): `TileDebugColor` — чистая функция состояние→цвет (по аналогии с `draw()` из `legacy/burmolda_demo.html`: старт/текущая позиция/переход зелёный→оранжевый→красный по `DecayProgress01`/разрушена — тёмная), `TileVisualState` (снимок состояния плиты для маппинга), `TunnelDebugVisual` (создаёт/обновляет/уничтожает примитивы `GameObject.CreatePrimitive(PrimitiveType.Cube)`, программно, без .prefab), `TunnelDebugVisualController` (тонкий `MonoBehaviour`-драйвер).
  - `asmdef Burmalda.DebugVisuals` с EditMode Test-сборкой; `.unity`/`.prefab` не добавлялись и не менялись.
  - `Assets/Scripts/README.md` дополнен строкой `DebugVisuals/` с пометкой, что это debug-инфраструктура вне очереди спринтов.
- Закрыт issue #62 (доработка #8): камера уезжала влево-вправо при диагональном движении по трейлу, т.к. `TunnelCameraFollow.ComputeTargetPosition` брал `Column` цели камеры из позиции игрока. Столбец цели камеры зафиксирован на центре ширины тоннеля (`WorldGridProjection.Width / 2` — уже была доступна как зависимость, тащить новую не пришлось); отставание по `Row` (`TrailingRowsBehindPlayer`) не менялось. Поворот камеры (`ComputeTargetRotation`) по-прежнему смотрит на реальную позицию игрока — камера продолжает визуально «провожать» игрока взглядом при боковом движении, просто сама больше не сдвигается влево-вправо.
  - `Movement/Tests/TunnelCameraFollowTests.cs`: тест `OnTrailAdvanced_LateralMove_...` переписан под новое поведение (X цели камеры не меняется при шаге вбок вместо прежнего «следует без задержки»); добавлен новый тест на зигзаг-путь через все столбцы ширины — X цели камеры не меняется ни на одном шаге. Остальные существующие тесты камеры не потребовали правок — во всех них путь игрока проходил ровно через центральный столбец (2 из 5), поэтому старое и новое поведение совпадали.
  - `SmoothingFactor`, `TrailingRowsBehindPlayer`, `heightOffset` не менялись; правка — только в теле `ComputeTargetPosition`.
