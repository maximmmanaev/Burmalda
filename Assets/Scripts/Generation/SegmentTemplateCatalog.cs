using System.Collections.Generic;

namespace Burmalda.Generation
{
    /// <summary>
    /// Каталог авторских шаблонов сегментов (PRD v7 §21, issue #78).
    /// Раскладка задаётся компактной ASCII-сеткой (см. <see cref="ParseRows"/>)
    /// — читаемо и достаточно для текущего этапа, пока нет визуального
    /// редактора уровней; строки идут от входа (первая строка) к выходу
    /// (последняя).
    ///
    /// Символ → <see cref="SegmentTileType"/>: '.' Open, '#' Blocked,
    /// 'l' Lava, 'L' Lever, 'g' LeverGate, 'm' ManaSource, 'k' KeySource,
    /// 'v' GateVaultKeySource (тайник за Воротами — сумма из
    /// <see cref="SegmentTemplate.GateVaultPurchases"/>, не 'k'), 'A' Altar,
    /// 'B' Boss. Пять ловушек Спринта 13a (issues #213–#217) — единственные
    /// ловушки в игре (владелец, 2026-09-05, «оставить только пять новых
    /// ловушек»: прежние 'p' Pit/'e' ExplosiveTrigger/'a' TimedTrapArrowTrigger/
    /// 'b' TimedTrapBladeTrigger удалены целиком, их места в раскладках
    /// заняты — механической заменой символов, без изменения самих
    /// раскладок — соответствующими новыми: 'w' ArrowWaveTrigger (был 'a'),
    /// 'x' BombTrigger (был 'e'), 't' BladeTactTrigger (был 'b'),
    /// 'r' FallingRockTrigger (был 'p'), 'f' LavaWaveTrigger (новый символ,
    /// не заменял старый — Лава-волна не существовала как старый тип).
    /// 'A'/'B' встречаются ТОЛЬКО в <see cref="AltarTemplate"/>/
    /// <see cref="BossTemplate"/> (владелец, 2026-09-04/05: детерминированный
    /// поток, не случайный отбор) — в остальных шаблонах <see cref="All"/>
    /// не используются и не должны появляться вновь.
    ///
    /// **Контент-долг**: число ниже — снимок на момент последней партии, не
    /// поддерживается вручную при каждом добавлении (см.
    /// <c>SegmentTemplateCatalogTests.All_TemplateNamesAreUnique</c> и
    /// соседние тесты файла за актуальным counted-in-code состоянием). На
    /// момент Партии 5 (см. ниже) — 59 шаблонов в <see cref="All"/> — давно
    /// выше минимума ~30 на Ярус, требуемого PRD до релиза (issue #78:
    /// "минимум ~30 авторских шаблонов на Ярус до релиза"). Покрывает весь
    /// диапазон сложности 1–5 плотнее, чем раньше — достаточно, чтобы
    /// селектор перестал видеть только пустые тир-1 раскладки на старте (см.
    /// <see cref="SegmentSelector"/>).
    /// Открытый вопрос той же задачи: часть исходных 13 шаблонов помечена
    /// тегом <see cref="SegmentRewardTag.Coins"/> — валюта, которая в
    /// забеге больше не начисляется (PRD v9, экономика "Мана как доход
    /// забега, Монеты только в Лагере") — решение, что с этим делать
    /// (перевести на <see cref="SegmentRewardTag.Mana"/> или завести
    /// нейтральный тег "без награды"), за владельцем. Точный релизный
    /// объём/узнаваемость шаблонов — по-прежнему предмет контент-чеклиста
    /// релиза (issue #111).
    ///
    /// Проходимость каждого шаблона проверена на этапе авторинга — см.
    /// <c>SegmentTemplateCatalogTests.AllTemplates_AreTraversable</c> (issue
    /// #78: "проверяется на этапе авторинга, не в рантайме").
    /// </summary>
    public static class SegmentTemplateCatalog
    {
        /// <summary>
        /// Правка по итогам ручной проверки владельца (2026-09-04, «Алтари и
        /// вход в Комнату — убрать из случайного пула»): PRD v9 требует
        /// РОВНО два Алтаря фиксированным потоком перед каждой Комнатой
        /// Босса — структура маршрута, не декорация, которую может выбрать
        /// или не выбрать <see cref="SegmentSelector"/>. Раньше "зал-алтаря"
        /// и "арена-босса" сидели в <see cref="All"/> наравне с остальными —
        /// именно это было настоящей причиной прошлого бага "вход в Комнату
        /// каждые ~4 ряда" (временный тестовый билд лишь ОБНАЖИЛ проблему
        /// частотой отбора, не создал её). Оба шаблона вынесены сюда
        /// отдельными полями — <see cref="Generation.SegmentRowProvider"/>
        /// применяет их напрямую по достижении границы Яруса
        /// (<c>SegmentGenerationController.RowsPerDifficultyStep</c>), минуя
        /// <see cref="SegmentSelector"/> целиком. Раскладки НЕ менялись, только
        /// место, откуда их берут.
        /// </summary>
        public static readonly SegmentTemplate AltarTemplate = new SegmentTemplate("зал-алтаря", 1, SegmentRewardTag.Coins, ParseRows(new[]
        {
            ".....",
            "..A..",
            ".....",
            ".....",
            ".....",
            ".....",
        }));

        /// <summary>См. doc-комментарий <see cref="AltarTemplate"/> — тот же вывод из пула, симметрично для входа в Комнату Босса.</summary>
        public static readonly SegmentTemplate BossTemplate = new SegmentTemplate("арена-босса", 5, SegmentRewardTag.Artifact, ParseRows(new[]
        {
            ".....",
            ".....",
            "..B..",
            ".....",
            ".....",
        }));

        public static IReadOnlyList<SegmentTemplate> All { get; } = new List<SegmentTemplate>
        {
            new SegmentTemplate("пустой-перегон", 1, SegmentRewardTag.Coins, ParseRows(new[]
            {
                ".....",
                ".....",
                ".....",
                ".....",
                ".....",
            })),

            new SegmentTemplate("жила-маны", 1, SegmentRewardTag.Mana, ParseRows(new[]
            {
                ".....",
                ".m.m.",
                ".....",
                ".m.m.",
                ".....",
            })),

            new SegmentTemplate("россыпь-ключей", 1, SegmentRewardTag.Keys, ParseRows(new[]
            {
                ".....",
                "..k..",
                ".k.k.",
                "..k..",
                ".....",
            })),

            new SegmentTemplate("разлом", 2, SegmentRewardTag.Coins, ParseRows(new[]
            {
                ".....",
                "..r..",
                "rrrrr",
                "..r..",
                ".....",
            })),

            new SegmentTemplate("коридор-со-стрелами", 2, SegmentRewardTag.Coins, ParseRows(new[]
            {
                ".....",
                "..w..",
                "wwwww",
                "..w..",
                ".....",
            })),

            new SegmentTemplate("зал-с-рычагом", 2, SegmentRewardTag.Artifact, ParseRows(new[]
            {
                ".....",
                "..L.#",
                "...#g",
                ".....",
                ".....",
            })),

            new SegmentTemplate("коридор-с-лезвиями", 3, SegmentRewardTag.Coins, ParseRows(new[]
            {
                ".....",
                "..t..",
                "ttttt",
                "..t..",
                ".....",
            })),

            new SegmentTemplate("взрывной-проход", 3, SegmentRewardTag.Coins, ParseRows(new[]
            {
                ".....",
                "..x..",
                "xxxxx",
                "..x..",
                ".....",
            })),

            new SegmentTemplate("узкий-проход", 3, SegmentRewardTag.Coins, ParseRows(new[]
            {
                ".....",
                "##...",
                "...##",
                "##...",
                ".....",
            })),

            new SegmentTemplate("смешанная-опасность", 4, SegmentRewardTag.Coins, ParseRows(new[]
            {
                ".....",
                "#.r.#",
                "lrtrl",
                "#.r.#",
                ".....",
            })),

            // Найдено на реальном билде (2026-09-01, живой забег до Яруса 6):
            // прежний ExplosiveTrigger ('e', сейчас 'x' BombTrigger после
            // механической замены символов 2026-09-05) на (1,1) целился в
            // (2,1)='l' — Tile.MarkLethalTrap(Explosion) поверх уже стоящей
            // Lava бросало InvalidOperationException в рантайме
            // (Tile.GuardAgainstConflictingRole, docs/wiki/changelog.md,
            // задача «двойные флаги на плитах»). Минимальный фикс тогда —
            // сдвинуть триггер на одну клетку вправо (col1→col2, был
            // открытым полом); раскладка сохранена без изменений при замене
            // символов. BombTrigger/FallingRockTrigger/LavaWaveTrigger не
            // целятся в соседние клетки вовсе (см. SegmentRowProvider.
            // ApplyTileType) — этот класс ошибки для них не воспроизводим.
            new SegmentTemplate("испытание", 5, SegmentRewardTag.Artifact, ParseRows(new[]
            {
                ".....",
                "#.xw#",
                "#lxt#",
                ".....",
                ".....",
            })),

            // ==================== Партия 2 (issue #111) ====================
            // 13 шаблонов, авторские раскладки владельца продукта. Правило
            // отбора по тирам и запрет повтора подряд — SegmentSelector
            // (задача «партии 1 и 2 + правила отбора»).

            // Тир 1. Первый длинный сегмент каталога (8 рядов) — контраст
            // темпа: после плотного участка нужна передышка, иначе
            // напряжение перестаёт читаться.
            new SegmentTemplate("длинный-перегон", 1, SegmentRewardTag.Mana, ParseRows(new[]
            {
                ".....",
                ".....",
                ".....",
                "..m..",
                ".....",
                ".....",
                ".....",
                ".....",
            })),

            // Тир 2. Выглядит пустым, одна яма ровно на естественной прямой —
            // учит, что пусто не значит безопасно.
            new SegmentTemplate("пустой-обман", 2, SegmentRewardTag.Mana, ParseRows(new[]
            {
                ".....",
                ".....",
                "rrrrr",
                ".....",
                "..m..",
            })),

            // Тир 2. Награды по очереди на разных краях, тянут игрока из
            // стороны в сторону.
            new SegmentTemplate("качели", 2, SegmentRewardTag.Mana, ParseRows(new[]
            {
                ".....",
                "m...m",
                "..#..",
                ".m.m.",
                "..#..",
                "..m..",
            })),

            // Тир 2. Ключи по краям, центр пуст — учит держаться стен.
            new SegmentTemplate("предбанник", 2, SegmentRewardTag.Keys, ParseRows(new[]
            {
                ".....",
                "k...k",
                ".....",
                "k...k",
                ".....",
                ".....",
            })),

            // Тир 2. Две полосы: слева триггеры Бомбы (риск без награды —
            // задевать не обязательно), справа Мана в чистой полосе.
            //
            // Задача «награда никогда не лежит на ловушке» (владелец, Спринт
            // «Стены вместо ловушек»): раньше Мана стояла в ЛЕВОЙ полосе,
            // прямо в области поражения трёх триггеров Бомбы — "триггер
            // уничтожает награду под собой" было подтверждено владельцем как
            // намеренная механика 2026-09-01, но это решение отменено новой
            // задачей ("награда — приманка к риску маршрута, а не капкан").
            // Мана перенесена в правую полосу (была "чистая, но пустая") —
            // позиции триггеров не менялись, исправление инварианта, не
            // авторство раскладки.
            new SegmentTemplate("развилка-цены", 2, SegmentRewardTag.Mana, ParseRows(new[]
            {
                "m....",
                "..#..",
                "x.#..",
                "..#xx",
                "xx#..",
                ".....",
            })),

            // Тир 3. Плотная россыпь ключей, вход и выход только с правого
            // края — внутри развернуться негде.
            new SegmentTemplate("богатый-карман", 3, SegmentRewardTag.Keys, ParseRows(new[]
            {
                ".....",
                ".###.",
                ".#kk.",
                ".#kk.",
                ".###.",
                ".....",
            })),

            // Тир 3. Рычаг на виду, ворота рядом — простейшая форма
            // механики: знакомит с правилом, не наказывая.
            // issue #208: клетка ворот примыкала к стенам со всех сторон,
            // кроме самого ключа за ней — при открытых воротах подойти к
            // ним было физически неоткуда. Один символ в стене (col2 у
            // ворот) открывает подход именно К ВОРОТАМ, не к ключу напрямую
            // — рычаг остаётся обязательным, карман больше не замкнут сам на себя.
            // Владелец, 2026-09-04: рычаг↔Ворота 2 ряда — короткий крюк,
            // тайник на 1 покупку ('v' = GateVaultKeySource, не 'k' — сумма
            // из GateVaultPricing, не общий KeysPerSource).
            new SegmentTemplate("двор-с-рычагом", 3, SegmentRewardTag.Keys, ParseRows(new[]
            {
                ".....",
                "...L.",
                "..###",
                "...gv",
                "..###",
                ".....",
            }), gateVaultPurchases: 1.0),

            // Тир 3. Рычаг в начале, ворота в конце — нажимаешь, ещё не зная,
            // что откроешь.
            // issue #208: та же правка, что у "двор-с-рычагом" — подход к
            // воротам сбоку, ключ по-прежнему достижим только через них.
            // Владелец, 2026-09-04: рычаг↔Ворота 4 ряда — длинный крюк,
            // тайник на 1.5 покупки.
            new SegmentTemplate("ворота-склада", 3, SegmentRewardTag.Keys, ParseRows(new[]
            {
                ".....",
                ".L...",
                ".....",
                ".....",
                "..###",
                "...gv",
                "..###",
            }), gateVaultPurchases: 1.5),

            // Тир 4. Ключевой шаблон партии: запертый склад стоит у входа,
            // рычаг лежит глубоко внизу. Чтобы забрать награду, надо пройти
            // мимо, спуститься за рычагом и вернуться назад по уже
            // прогорающему полу — ближайшее к механике Ворот из PRD v9 §4.3,
            // что можно собрать на текущих Lever/LeverGate.
            // issue #208: та же правка — подход к воротам сбоку (col2),
            // не к ключу напрямую.
            // Владелец, 2026-09-04: рычаг↔Ворота 4 ряда — длинный крюк,
            // тайник на 1.5 покупки.
            new SegmentTemplate("рычаг-в-глубине", 4, SegmentRewardTag.Keys, ParseRows(new[]
            {
                "..###",
                "...gv",
                "..###",
                ".....",
                ".....",
                "..L..",
                ".....",
            }), gateVaultPurchases: 1.5),

            // Тир 4. Два триггера сторожат подход к ключу: сработает любой, и
            // коридор к награде сузится (взрыв делает плиту непроходимой
            // дырой, issue #260 — в этом и есть "сужение коридора", не
            // угроза самому ключу).
            //
            // Задача «награда никогда не лежит на ловушке»: раньше вокруг
            // ключа стояло СЕМЬ дополнительных триггеров (xxkxx/.xxx.),
            // из-за чего ключ был в области поражения пяти из них — не
            // соответствовало даже собственному описанию строки выше ("два
            // триггера"). Лишние семь удалены, оставлены исходные два
            // (owner-текст был точен про них изначально) — восстанавливает
            // и описание, и инвариант одним и тем же движением, не
            // авторство новой раскладки.
            new SegmentTemplate("караул", 4, SegmentRewardTag.Keys, ParseRows(new[]
            {
                ".....",
                ".x.x.",
                ".....",
                "..k..",
                ".....",
                ".....",
            })),

            // Тир 4. Узкий перешеек между лавой, уклоняться некуда.
            //
            // issue #251 (владелец, плейтест Ярус 3): третий ряд ("lllll")
            // был сплошной лавой без единого прохода — статичная Лава
            // становится LethalTrap уже на генерации, TryAdvanceTo
            // категорически отклоняет шаг на неё, так что этот ряд был
            // гарантированным тупиком, а не риском. Механическая правка —
            // открыт тот же центральный столбец, что уже открыт в двух рядах
            // выше (см. SegmentReachabilityValidatorTests.IsTraversable_StaticLavaWithNoGap_ReturnsFalse
            // и docs/wiki/traps.md), НЕ ослабление узости перешейка — сама
            // ширина прохода (1 плита) не изменилась, изменилось только то,
            // что этот единственный проход теперь физически существует на
            // всех трёх рядах, а не на двух из трёх.
            new SegmentTemplate("лавовый-коридор", 4, SegmentRewardTag.Mana, ParseRows(new[]
            {
                ".....",
                "ll.ll",
                "ll.ll",
                "ll.ll",
                ".....",
                "..m..",
            })),

            // Тир 5. Четыре ключа, ямы между ними — самая богатая и самая
            // опасная раскладка каталога.
            new SegmentTemplate("щедрый-риск", 5, SegmentRewardTag.Keys, ParseRows(new[]
            {
                ".....",
                ".r.r.",
                ".k.k.",
                "rrrrr",
                ".k.k.",
                ".r.r.",
                ".....",
            })),

            // Тир 5. Диагональная цепь Маны через всю ширину — чтобы собрать
            // всё, маршрут получается максимально длинным, а значит и распад
            // максимальным.
            new SegmentTemplate("двойная-жила", 5, SegmentRewardTag.Mana, ParseRows(new[]
            {
                ".....",
                "m....",
                ".m...",
                "..m..",
                "...m.",
                "....m",
            })),

            // ==================== Партия 1 (issue #111) ====================
            // 14 шаблонов, авторские раскладки владельца продукта — написана
            // раньше Партии 2, интегрирована позже (очередь задач
            // перетасовалась). Отдельная секция ниже, а не переставлена
            // хронологически внутрь Партии 2 выше — порядок добавления в
            // каталог не несёт игрового смысла (SegmentSelector выбирает по
            // тиру, не по позиции в списке), сохранён для истории коммитов.

            // Тир 1. Первое решение в игре: две полосы, две валюты, взять обе не выйдет.
            new SegmentTemplate("выбор-полосы", 1, SegmentRewardTag.Mana, ParseRows(new[]
            {
                ".....",
                "m...k",
                ".....",
                "m...k",
                ".....",
            })),

            // Тир 1. Прямая дорога перекрыта по центру, учит сворачивать.
            new SegmentTemplate("первый-крюк", 1, SegmentRewardTag.Mana, ParseRows(new[]
            {
                ".....",
                "..#..",
                ".m.k.",
                "..#..",
                ".....",
            })),

            // Тир 2. Центральная полоса выглядит удобной и наказывает: оба
            // триггера бьют по ней.
            new SegmentTemplate("ложная-прямая", 2, SegmentRewardTag.Mana, ParseRows(new[]
            {
                ".....",
                "..x..",
                "xxxxx",
                "..x..",
                ".....",
            })),

            // Тир 2. Ключ кажется замурованным, достижим только с правого края.
            new SegmentTemplate("жадный-угол", 2, SegmentRewardTag.Keys, ParseRows(new[]
            {
                ".....",
                "..rr.",
                "rrrk.",
                "..rrr",
                ".....",
            })),

            // Тир 2. Обе жилы стоят между ямами.
            new SegmentTemplate("двойная-цена", 2, SegmentRewardTag.Mana, ParseRows(new[]
            {
                ".....",
                ".r.r.",
                "rm.m.",
                "rrrrr",
                ".....",
            })),

            // Тир 3. Карман справа изолирован стеной, рычаг у входа: решать
            // надо заранее, ещё не видя содержимого.
            new SegmentTemplate("пещера-за-рычагом", 3, SegmentRewardTag.Keys, ParseRows(new[]
            {
                ".....",
                ".L.#.",
                "...#g",
                "...#k",
                "...#.",
            })),

            // Тир 3. Лезвия по краям, затем по центру: ритм, а не реакция.
            new SegmentTemplate("коридор-лезвий", 3, SegmentRewardTag.Mana, ParseRows(new[]
            {
                ".....",
                "t...t",
                "ttttt",
                ".t.t.",
                ".....",
            })),

            // Тир 3. Вход в лавовое поле один, с правого края.
            //
            // issue #251 (владелец, плейтест Ярус 3): четвёртый ряд
            // (".llll") случайно заливал лавой и правый край тоже — тот
            // самый "единственный вход с правого края", который держит связь
            // входа и выхода во ВСЕХ остальных рядах шаблона. Ряд входа
            // (0) был отрезан от ряда выхода (4) целиком: столбец 0 в рядах
            // 1/3 — изолированный карман (ряд 2 слева сплошная лава), а
            // единственный сквозной путь через правый край (столбец 4)
            // обрывался именно на этом ряду. Механическая правка — открыт
            // столбец 4, как и в ряду выше ("..lll." → тот же паттерн, что
            // ряд 1), сама плотность лавы не менялась.
            new SegmentTemplate("ключ-за-лавой", 3, SegmentRewardTag.Keys, ParseRows(new[]
            {
                ".....",
                ".lll.",
                "lllk.",
                ".lll.",
                ".....",
            })),

            // Тир 4. Проходится только зигзагом, то есть с максимальным
            // распадом за спиной.
            new SegmentTemplate("решето", 4, SegmentRewardTag.Mana, ParseRows(new[]
            {
                ".....",
                "r.r.r",
                "rrrrr",
                "r.r.r",
                "..m..",
            })),

            // Тир 4. Плотный узел триггеров Бомбы держит центр — ключ в
            // стороне, риск задеть узел добровольный, не по пути к награде.
            //
            // Задача «награда никогда не лежит на ловушке»: раньше ключ
            // стоял в самом центре узла триггеров ("прямой путь уничтожает
            // собственную награду" — подтверждено владельцем как намеренная
            // механика 2026-09-01, отменено новой задачей). `Generation.
            // SegmentTemplate.ValidateTriggerTargetsDoNotConflict`, на
            // который ссылалась прежняя версия этого комментария, к этому
            // моменту уже удалена как мёртвый код (владелец, 2026-09-05,
            // «оставить только пять новых ловушек» — см. её историческую
            // заметку в SegmentTemplate.cs) — ссылка была неактуальна ещё до
            // этой задачи. Ключ перенесён в угол вне области поражения узла
            // — позиции триггеров не менялись.
            new SegmentTemplate("выкуп", 4, SegmentRewardTag.Keys, ParseRows(new[]
            {
                "...k.",
                ".x...",
                "x.xxx",
                "xxx..",
                ".....",
            })),

            // Тир 4. Богатая середина, стрелы по краям.
            //
            // Задача «награда никогда не лежит на ловушке»: волна Стрелы
            // рано или поздно накрывает ВЕСЬ ряд триггера (столбец за
            // столбцом от края до края, ArrowWaveTrapSystem), поэтому Мана в
            // ряду 1 была в области поражения независимо от столбца — сдвиг
            // по горизонтали не спасал бы. Перенесена в ряд 3, где уже была
            // соседняя Мана (не задет ни один триггер) — позиции w не
            // менялись.
            new SegmentTemplate("стрелы-и-жила", 4, SegmentRewardTag.Mana, ParseRows(new[]
            {
                ".....",
                "w...w",
                "wwwww",
                ".mmm.",
                ".....",
            })),

            // Тир 5. Единственная проходимая колонна, уклоняться некуда.
            new SegmentTemplate("мост", 5, SegmentRewardTag.Mana, ParseRows(new[]
            {
                ".....",
                "rr.rr",
                "rrrrr",
                "rr.rr",
                "..m..",
            })),

            // Тир 5. Два ключа за полосой Лавы/Бомбы — риск в самой полосе,
            // не в награде за ней.
            //
            // Задача «награда никогда не лежит на ловушке»: раньше ключи
            // стояли прямо под триггерами Бомбы ("триггер уничтожает
            // награду" — та же отменённая механика, что у «выкуп» выше).
            // Сдвинуты на ряд дальше (был пустым), позиции l/x не менялись.
            new SegmentTemplate("последний-рывок", 5, SegmentRewardTag.Keys, ParseRows(new[]
            {
                ".....",
                ".l.l.",
                "lxlxl",
                ".....",
                ".k.k.",
            })),

            // Тир 5. Богатый ствол со стенами: зашёл — идёшь до конца.
            new SegmentTemplate("шахта-жадности", 5, SegmentRewardTag.Keys, ParseRows(new[]
            {
                ".....",
                ".#k#.",
                ".#k#.",
                ".#k#.",
                ".....",
            })),

            // ==================== Партия 3 (баг с устройства, 2026-09-04) ====================
            // Пять ловушек Спринта 13a (issues #213–#217) были написаны и
            // покрыты тестами, но без символов шаблона не могли попасть на
            // трассу — см. doc-комментарий SegmentTileType.ArrowWaveTrigger.
            // Существующие раскладки НЕ переписаны — только новые три.

            // Тир 2. Стрелы новой волновой механики (issue #213) — весь ряд
            // триггера станет опасным столбец за столбцом через 1 ход, плюс
            // падающий камень (issue #217) двумя клетками ниже.
            new SegmentTemplate("рой-стрел", 2, SegmentRewardTag.Mana, ParseRows(new[]
            {
                ".....",
                "..w..",
                "rrrrr",
                ".r.r.",
                "..m..",
            })),

            // Тир 3. Такт Лезвий (issue #215) — пятитактовый паттерн, ряд
            // триггера начинает становиться опасным через несколько ходов.
            new SegmentTemplate("такт-лезвий-волной", 3, SegmentRewardTag.Keys, ParseRows(new[]
            {
                ".....",
                ".t.t.",
                "ttttt",
                "..t..",
                "..k..",
            })),

            // Тир 4. Бомба (issue #214, площадь 3×3 через 2 хода) и волна
            // Лавы (issue #216, идёт назад от триггера — инвариант волны
            // сам не даст ей перекрыть ряд игрока/впереди него, см.
            // Movement.LavaWaveTrapSystem).
            new SegmentTemplate("бомба-и-поток-лавы", 4, SegmentRewardTag.Mana, ParseRows(new[]
            {
                ".....",
                "..x..",
                "xxxxx",
                "..f..",
                ".m.m.",
            })),

            // ==================== Партия 4 (владелец, 2026-09-05, «новые ловушки встречаются слишком редко») ====================
            // Партия 3 (выше) заводила по одному образцу каждого из пяти
            // типов — 3 шаблона из тогдашних 43, ~7% каталога, все пять типов
            // встречались ровно один раз. Восемь новых шаблонов ниже доводят
            // суммарно до одиннадцати из сорока одного в случайном пуле
            // (~27%) — каждый из пяти типов теперь в 3-4 разных шаблонах
            // разных тиров. Существующие раскладки не тронуты.

            // Тир 1. Первое знакомство с волной Стрел — одна, посреди пути.
            new SegmentTemplate("стрелы-на-переправе", 1, SegmentRewardTag.Mana, ParseRows(new[]
            {
                ".....",
                "..w..",
                ".....",
                ".....",
                "..m..",
            })),

            // Тир 2. Два падающих камня по бокам прохода.
            new SegmentTemplate("камни-в-нише", 2, SegmentRewardTag.Mana, ParseRows(new[]
            {
                ".....",
                ".....",
                "rrrrr",
                ".....",
                "..m..",
            })),

            // Тир 2. Такт Лезвий по краям — центр остаётся безопасным дольше.
            new SegmentTemplate("клинки-по-контуру", 2, SegmentRewardTag.Keys, ParseRows(new[]
            {
                ".....",
                "t...t",
                "ttttt",
                ".....",
                "..k..",
            })),

            // Тир 3. Три триггера Бомбы — площади взрыва почти перекрывают весь ряд.
            //
            // Задача «награда никогда не лежит на ловушке»: ключ стоял
            // прямо под нижним триггером (радиус захватывал его) — сдвинут
            // на тот же ряд, в угол вне радиуса; позиции x не менялись.
            new SegmentTemplate("мины-в-проходе", 3, SegmentRewardTag.Keys, ParseRows(new[]
            {
                ".....",
                "x...x",
                "xxxxx",
                "..x..",
                "k....",
            })),

            // Тир 3. Волна Лавы у развилки — инвариант волны (не задевает
            // ряд игрока/впереди) делает её безопасной для авторинга без
            // ручного расчёта тайминга, см. Movement.LavaWaveTrapSystem.
            new SegmentTemplate("разлив-у-развилки", 3, SegmentRewardTag.Mana, ParseRows(new[]
            {
                ".....",
                "..f..",
                "fffff",
                ".....",
                "..m..",
            })),

            // Тир 4. Стрелы и Бомба в одном сегменте — два разных момента опасности.
            //
            // Задача «награда никогда не лежит на ловушке»: обе Маны были в
            // радиусе поражения центрального триггера Бомбы — раздвинуты к
            // краям того же ряда, вне радиуса; позиция x не менялась.
            new SegmentTemplate("перекрёстный-огонь", 4, SegmentRewardTag.Mana, ParseRows(new[]
            {
                ".....",
                "..w..",
                "wwwww",
                "..x..",
                "m...m",
            })),

            // Тир 4. Падающие камни ведут к такту Лезвий.
            new SegmentTemplate("камнепад-и-лезвия", 4, SegmentRewardTag.Keys, ParseRows(new[]
            {
                ".....",
                ".r.r.",
                "rrrrr",
                "..t..",
                "..k..",
            })),

            // Тир 5. Волна Лавы и Бомба вместе — самый насыщенный из новых.
            //
            // Задача «награда никогда не лежит на ловушке»: ключ был в
            // радиусе поражения триггера Бомбы — сдвинут в тот же ряд, вне
            // радиуса; позиция x не менялась.
            new SegmentTemplate("огненная-теснина", 5, SegmentRewardTag.Keys, ParseRows(new[]
            {
                ".....",
                "..f..",
                "fffff",
                "..x..",
                "....k",
            })),

            // ==================== Партия 5 (владелец, задача 3 спринта «Стены
            // вместо ловушек»: «нужно сделать побольше стен, чтобы игрок не
            // мог просто идти прямо») ====================
            // Десять шаблонов ниже — геометрия как основной инструмент
            // сложности, не количество ловушек (продолжение той же задачи,
            // что развернула правило избегаемости в Задаче 1 и убрала
            // капканы-на-награде в Задаче 2). Раскладки и комментарии-замыслы
            // — владельца, дословно из постановки задачи. Обязательная по
            // постановке сверка (не только заявленная владельцем «вручную
            // проверенная чистота пути») — прогнаны программно все три
            // проверки авторинга на каждый шаблон этой партии:
            // SegmentReachabilityValidator.IsTraversable,
            // SegmentReachabilityValidator.HasAvoidableRoute (Задача 1) и
            // RewardTrapConflictValidator.FindConflicts (Задача 2) — все
            // десять проходят все три без единой правки раскладки.

            // Тир 1. Первая стена в игре, учит, что прямо не всегда можно.
            new SegmentTemplate("первая-стена", 1, SegmentRewardTag.Mana, ParseRows(new[]
            {
                ".....",
                "..#..",
                ".....",
                "..#..",
                ".....",
            })),

            // Тир 1. Проход в одну плиту, идти можно только гуськом.
            new SegmentTemplate("горло", 1, SegmentRewardTag.Mana, ParseRows(new[]
            {
                ".....",
                "##.##",
                ".....",
                "##.##",
                ".....",
            })),

            // Тир 2. Окно прохода смещается на плиту каждый ряд: пройти
            // можно только по диагонали через всю ширину.
            new SegmentTemplate("диагональ", 2, SegmentRewardTag.Mana, ParseRows(new[]
            {
                ".....",
                "..###",
                "#..##",
                "##..#",
                "###..",
                ".....",
            })),

            // Тир 2. Мана лежит в правом рукаве, рядом волна стрел. Взять
            // можно, обойдя триггер; левый край чист и пуст.
            new SegmentTemplate("короткий-путь", 2, SegmentRewardTag.Mana, ParseRows(new[]
            {
                ".....",
                ".##..",
                ".##w.",
                ".##m.",
                ".##..",
                ".....",
            })),

            // Тир 3. Три полосы, средняя короче и с ловушкой, крайние длиннее
            // и чисты.
            new SegmentTemplate("клык", 3, SegmentRewardTag.Mana, ParseRows(new[]
            {
                ".....",
                ".#.#.",
                ".#w#.",
                ".#.#.",
                ".....",
            })),

            // Тир 3. Ключи в центре, на выходе горло в одну плиту: забрал —
            // протискивайся.
            new SegmentTemplate("тиски", 3, SegmentRewardTag.Keys, ParseRows(new[]
            {
                ".....",
                "#...#",
                "#.k.#",
                "#...#",
                "##.##",
                ".....",
            })),

            // Тир 4. Две волны стрел по бокам, чистый коридор ровно по
            // центру.
            new SegmentTemplate("двойной-фронт", 4, SegmentRewardTag.Mana, ParseRows(new[]
            {
                ".....",
                ".w.w.",
                ".....",
                "#...#",
                ".#.#.",
                ".....",
            })),

            // Тир 4. Камень падает в середине единственного широкого места.
            new SegmentTemplate("коридор-камней", 4, SegmentRewardTag.Keys, ParseRows(new[]
            {
                ".....",
                "#.#.#",
                "..r..",
                "#.#.#",
                ".....",
            })),

            // Тир 5. Статичная лава сжимает проход, триггер волны лавы стоит
            // в глубине правого рукава.
            new SegmentTemplate("лавовый-рукав", 5, SegmentRewardTag.Mana, ParseRows(new[]
            {
                ".....",
                ".lll.",
                ".l.l.",
                ".lf..",
                ".lll.",
                ".....",
            })),

            // Тир 5. Горло, за ним две бомбы по краям, за ними награда.
            new SegmentTemplate("последняя-миля", 5, SegmentRewardTag.Mana, ParseRows(new[]
            {
                ".....",
                "##.##",
                ".x.x.",
                "##.##",
                ".m.m.",
                ".....",
            })),
        };

        private static SegmentTileType[,] ParseRows(string[] rows)
        {
            var rowCount = rows.Length;
            var width = rows[0].Length;
            var tiles = new SegmentTileType[rowCount, width];

            for (var r = 0; r < rowCount; r++)
            for (var c = 0; c < width; c++)
                tiles[r, c] = rows[r][c] switch
                {
                    '.' => SegmentTileType.Open,
                    '#' => SegmentTileType.Blocked,
                    'l' => SegmentTileType.Lava,
                    'L' => SegmentTileType.Lever,
                    'g' => SegmentTileType.LeverGate,
                    'm' => SegmentTileType.ManaSource,
                    'k' => SegmentTileType.KeySource,
                    'v' => SegmentTileType.GateVaultKeySource,
                    'A' => SegmentTileType.Altar,
                    'B' => SegmentTileType.Boss,
                    'w' => SegmentTileType.ArrowWaveTrigger,
                    'x' => SegmentTileType.BombTrigger,
                    't' => SegmentTileType.BladeTactTrigger,
                    'r' => SegmentTileType.FallingRockTrigger,
                    'f' => SegmentTileType.LavaWaveTrigger,
                    _ => SegmentTileType.Open
                };

            return tiles;
        }
    }
}
