namespace Burmalda.Core
{
    /// <summary>
    /// Тип смертельной статичной ловушки (PRD 4.2: «ямы, лава — видны заранее,
    /// нужно обходить»). В отличие от <see cref="Tile.IsBlocked"/> (плита
    /// физически непроходима, без последствий), попытка наступить на плиту с
    /// таким типом должна приводить к смерти — см. <c>GridTraceTrail.LethalTrapTriggered</c>.
    /// Оба варианта ведут себя одинаково механически (мгновенная смерть, PRD
    /// 9 про d20-испытание пока не реализовано, см. Burmalda.RunLifecycle) —
    /// разделены только для будущей тематической/визуальной разницы.
    /// </summary>
    public enum LethalTrapType
    {
        Pit,
        Lava
    }
}
